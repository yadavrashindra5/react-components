import { PropsInfo } from "../Props";

export const accordionProps: PropsInfo = {
  headers: ["ID", "Name", "Type", "Description"],
  caption:
    "The Modal component renders a modal dialog box and accepts child elements for its content.",
  propsDetail: [
    
  ],
};
